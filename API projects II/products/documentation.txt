********************** DAD JOKES **********************
Always test the URL in the browser before starting 
to code.
API = external source of data

****
DOCS
****

*****
FETCH
*****
const url =  'https://icanhazdadjoke.com/';

../...
btn.addEventListener('click', () => {
    fetchDadJoke();
})

const fetchDadJoke = async() => {
    result.textContent = 'Loading...'
  try{
    const response = await fetch(url, {
        headers:{
            Accept: 'application/json',
            'User-Agent': 'learning app'
        }
    })
    if(!response.ok){
        throw new Error('There was an error')
    }
    const data = await response.json();
    log(data);
    result.textContent = data.joke;
  }catch(error){
    log(error.message);
    result.textContent = error.message;
  }
}; 

********************** PRODUCTS **********************

***********
AJAX STATES
***********

3 possible states for an AJAX application:
- Loading
- Display
- Error

If Loading is succesfull then Display
If Loading is not sccessfull catch Error 


****
DOCS
****

course-api.com => javascript store products,
                  javascript store single product

*****
FETCH
*****
If you are in web site "abc.com" you are not allowed to fetch data 
from the same site. 
  
CORS = Cross Origin Resource Sharing   

To solve the problem:
    1 Type in the browser (Chrome):                  
const url = 'https://cors-anywhere.herokuapp.com/https://course-api.com/javascript-store-products';

    2 If it is not enough type a second request:
    http://cors-anywhere.herokuapp.com/corsdemo   to get temporary access.

To get the id of a product:
     const {id} = product;
     log(id);